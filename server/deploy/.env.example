# 本地开发用中间件口令模板。
#
# 使用方式：复制为同目录下的 .env 后再启动容器。
#   cp server/deploy/.env.example server/deploy/.env
#
# .env 已被 .gitignore 排除，任何情况下都不要提交。
# 生产环境不得使用这里的占位值。

MYSQL_ROOT_PASSWORD=change-me
MYSQL_DATABASE=bilibili_web
